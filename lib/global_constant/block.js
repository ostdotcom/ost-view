"use strict";

const blocks = {

  // Verified enum types Start //
  unverified: 'unverified',

  verified: 'verified',

  failed: 'failed',

  // Verified enum types End //

};

module.exports = blocks;